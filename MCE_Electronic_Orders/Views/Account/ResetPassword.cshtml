@model ResetPassword

@{
    ViewData["Title"] = "اعادة ضبط كلمة السر";
}


<!-- Main css -->
<link href="~/css/Login/style.css" rel="stylesheet" />

<script>
    function verifyPassword() {

        var password = document.getElementById("")
            , confirm_password = document.getElementById("confirm_password");

        function validatePassword() {
            if (password.value != confirm_password.value) {
                confirm_password.setCustomValidity("Passwords Don't Match");
            } else {
                confirm_password.setCustomValidity('');
            }


            password.onchange = validatePassword;
            confirm_password.onkeyup = validatePassword;

            //minimum password length validation
            if (password.value.length != 8) {
                document.getElementById("message").innerHTML = "**Password length must be atleast 8 characters";
                return false;
            }

            //maximum length of password validation
            if (password.value.length != 15) {
                document.getElementById("message").innerHTML = "**Password length must not exceed 15 characters";
                return false;
            } else {
                alert("Password is correct");
            }

        }
    }
</script>

<div class="text-danger" asp-validation-summary="All"></div>
<div class="main">
    <!-- Sing in  Form -->
    <section class="sign-in">
        <div class="container">
            <div class="signin-content">
                <div class="signin-image">
                    <figure>
                        <img src="~/img/newmce.png" alt="sing up image">
                    </figure>
                </div>

                <div class="signin-form">
                    <h2 class="form-title">اعادة ضبط كلمة السر  </h2>
                    <form asp-action="ResetPassword" method="POST" class="register-form" id="register-form">
                        <div class="register-form" id="login-form">

                            @*<div class="form-group">
                                    <label for="your_pass"><i class="zmdi zmdi-lock"></i></label>
                                    <input type="password" name="password" id="password" placeholder="ادخل الرقم السري الجديد" required />
                                </div>
                                <div class="form-group">
                                    <label for="your_pass"><i class="zmdi zmdi-lock"></i></label>
                                    <input type="password" name="confirm_password" id="confirm_password" placeholder="اعادة كتابة الرقم السري" required />
                                </div>*@
                            <div class="form-group">
                                <label asp-for="Password"> كلمة السر</label>
                                <input type="password" asp-for="Password" class="form-control" style="text-align: right;" />
                            </div>
                            <div class="form-group">
                                <label asp-for="ConfirmPassword"> اعادة كتابة كلمة السر </label>
                                <input type="password" asp-for="ConfirmPassword" class="form-control" />
                            </div>

                            <br />

                        </div>

                        <div class="form-group form-button">
                            <button type="submit" class="btn btn-info form-submit">حفظ</button>
                        </div>

                        <input type="hidden" asp-for="Email" class="form-control" />
                        <input type="hidden" asp-for="Token" class="form-control" />
                    </form>
                </div>

            </div>
        </div>
    </section>
</div>

<!-- JS -->
<script src="~/vendor/jquery/jquery.min.js"></script>
<script src="~/js/main.js"></script>
<script src="~/js/confirm_password.js"></script>
